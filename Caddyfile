{$DOMAIN} {
	encode gzip

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
	}

	# Internal compose network — not the app's 127.0.0.1:3000 host mapping.
	reverse_proxy app:3000
}
